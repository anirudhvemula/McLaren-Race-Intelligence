# McLaren Race Intelligence Platform — ADK Agent Dependencies
# Pinned to the ADK 1.x era (spring 2026, around Google Cloud Next).
# ADK 2.0 GA'd 2026-05-19; this lab targets the 1-series, so every dependency
# is capped below its next major boundary to avoid post-1.x breaking changes.

google-adk>=1.36,<2.0
google-cloud-bigquery>=3.41,<4
pandas>=3.0,<4
db-dtypes>=1.5,<2
google-cloud-aiplatform[agent_engines,adk]>=1.148,<2
